apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "payment-service.fullname" . }}
spec:
{{- if .Values.gateway.enabled }}
  parentRefs:
  - name: {{ include "payment-service.fullname" . }}-gateway
{{- end }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: {{ .Values.route.path }}
    backendRefs:
    - name: {{ include "payment-service.fullname" . }}
      port: {{ .Values.service.port }}
