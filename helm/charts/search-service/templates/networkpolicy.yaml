apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "search-service.fullname" . }}
spec:
  podSelector:
    matchLabels:
      app: {{ include "search-service.fullname" . }}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: tripkar-api-gateway
  egress:
  - {}
