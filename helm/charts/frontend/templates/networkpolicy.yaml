apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "frontend.fullname" . }}
spec:
  podSelector:
    matchLabels:
      app: {{ include "frontend.fullname" . }}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - {}
  egress:
  - {}
