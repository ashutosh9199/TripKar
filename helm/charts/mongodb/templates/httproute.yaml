apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "mongodb.fullname" . }}
spec:
{{- if .Values.gateway.enabled }}
  parentRefs:
  - name: {{ include "mongodb.fullname" . }}-gateway
{{- end }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: {{ .Values.route.path }}
    backendRefs:
    - name: {{ include "mongodb.fullname" . }}
      port: {{ .Values.service.port }}
