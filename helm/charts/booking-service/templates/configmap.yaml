apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "booking-service.fullname" . }}-config
data:
  NODE_ENV: "production"
  PORT: "{{ .Values.service.port }}"
  MONGO_URI: "mongodb://admin:secretpassword123@mongodb:27017"
